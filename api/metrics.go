package api

import (
	"net/http"
)

func (api *API) GetMetricsConfig(w http.ResponseWriter, r *http.Request) {
	//resp, err := api.services.GetMetricsConfig()
	//if err != nil {
	//	log.Error("api.services.GetConfig", zap.Error(err))
	//	response.JsonError(w, err)
	//	return
	//}
	//response.Json(w, json.RawMessage(resp))
}
